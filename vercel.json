{
  "buildCommand": "export DATABASE_URL=${DATABASE_URL:-${POSTGRES_URL_NON_POOLING:-${POSTGRES_PRISMA_URL:-$POSTGRES_URL}}}; [ -n \"$DATABASE_URL\" ] || (echo 'Missing database env. Set DATABASE_URL or Vercel Postgres vars.' && exit 1); prisma generate && prisma db push && next build"
}
